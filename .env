# =====================
# eAssist v10 — .env
# =====================
# Server
PORT=8080
NODE_ENV=production

# Database (custom user 'eassist')
# Make sure the 'eassist' role exists with CREATEDB on your server, or run:
#   createuser eassist --pwprompt
#   createdb -O eassist eassist
DATABASE_URL=postgres://eassist:Ea55157%212025@localhost:5432/eassist

# Auth (JWT)
JWT_SECRET=virK3RFR5kNiFUk0ajrvbEg8dvFHoYcikn8MLsiQ15zGTrVy33oQcHSWZif3YchE
JWT_REFRESH_SECRET=a5gyVxoIBVzjGCiE0iAcsHm5QMH3gC9yiqBjhZOWViBRUvTP70igE8PoZrRRmczy
ACCESS_TOKEN_TTL_MIN=30
REFRESH_TOKEN_TTL_DAYS=7

# Seeded admin (used by npm run seed:initial / setup:all)
ADMIN_EMAIL=admin@eassist.local
ADMIN_PASSWORD=EAVHHxmTAA9mwVE0OlWiqh
ADMIN_FULL_NAME='System Administrator'

# CORS (optional, comma-separated origins)
CORS_ORIGINS=http://localhost:3000

# Logging (optional): silent | error | warn | info | debug
LOG_LEVEL=info

# Optional: Public URL for emails/links
PUBLIC_APP_URL=http://localhost:3000

# Optional: File storage base (used by uploads if you add them)
STORAGE_DIR=./storage

# Optional: SMTP defaults for local testing (DB-backed settings take precedence in app UIs)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_SECURE=false
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_FROM_EMAIL=noreply@eassist.local
SMTP_FROM_NAME='eAssist Support'
